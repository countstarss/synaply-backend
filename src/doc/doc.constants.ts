export enum DocTypeValue {
  DOCUMENT = 'DOCUMENT',
  FOLDER = 'FOLDER',
}

export enum DocChangeSourceValue {
  CREATE = 'CREATE',
  EDITOR = 'EDITOR',
  META = 'META',
  SYSTEM = 'SYSTEM',
  IMPORT = 'IMPORT',
}
